#include <stdio.h>
#include <stdlib.h>

int sumOfAllDigits(int n) {
    int total_sum = 0;
    }
    return n;
}

int main(){
    int n, arr[100], tatal_sum;
    scanf("%d", &n);
    for(int i=0; i<=n; i++){
        scanf("%d", &arr[i]);
    }
    printf("1. 所有位數的和: %d\n", sumOfAllDigits(n));

    return 0;
}